# Initial Baseline Pending

The initial production schema has not been dumped into this repository yet.

Do not hand-write `0001_initial_baseline.sql` from memory or from TypeScript code. The app already has live Supabase tables, policies, and columns that must be captured from the actual database.

Follow `supabase/migrations/README.md` to generate:

```text
supabase/migrations/0001_initial_baseline.sql
```

Once that file is committed, delete this placeholder.
